spring:
  profiles:
    active: local
  cloud:
    config:
      uri: ${spring.cloud.config.uri}

---
spring:
  profiles: local
  cloud:
    config:

      #uri: ${SPRING_CLOUD_CONFIG}
      uri: http://localhost:2001

